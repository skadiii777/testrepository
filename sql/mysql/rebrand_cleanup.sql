-- ----------------------------------------------------------------------------
-- 去上游品牌残留清理（数据级，幂等可重复执行）
-- 背景：yudao 种子数据的根部门「芋道源码」会出现在注册页部门下拉、注册审批列表等界面。
-- 用法：在 ruoyi-vue-pro.sql 基础种子导入之后执行本脚本。
-- 范围：仅改显示名，不改动任何 id / 层级关系，用户 dept_id 绑定不受影响。
-- ----------------------------------------------------------------------------

-- 1) 根部门：芋道源码 -> 集团总部
UPDATE `system_dept`
SET `name` = '集团总部', `updater` = '1', `update_time` = NOW()
WHERE `id` = 100 AND `name` = '芋道源码';

-- 2) 校验：以下查询应返回空
-- SELECT id, name FROM system_dept WHERE name LIKE '%芋道%' OR name LIKE '%若依%';
